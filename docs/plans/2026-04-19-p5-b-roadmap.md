# P5.B OSC Dispatch Roadmap

## Goal

Turn `P5.B` into bounded OSC stream-dispatch slices instead of treating all OSC
semantic routing as one broad task.

## Upstream scope

- `upstream/ghostty/src/terminal/stream.zig`
  - `oscDispatch`
- `upstream/ghostty/src/terminal/osc.zig`

## Dependency notes

- `P5.B` depends on the current translated `osc.Command` surface.
- `P4.C` and `P4.D` can still extend `osc.Command` later. When they do, `P5.B`
  must grow with them through new bounded sub-slices rather than reopening a
  vague umbrella implementation turn.

## Sub-slices

### P5.B1 Current translated OSC core

Wire the OSC commands already translated in `src/terminal/osc.mbt`:

- window title
- window icon as intentional no-op
- report pwd
- mouse shape
- color operation
- invalid OSC as silent internal no-op

Status:

- done
- implementation audit:
  [2026-04-19-p5-b-current-osc-core.md](/Users/haoxiang/Workspace/moonbit/feihaoxiang/ghostty/docs/plans/2026-04-19-p5-b-current-osc-core.md)

### P5.B2 Hyperlink dispatch after `P4.C1`

Wire the translated hyperlink commands from `OSC 8`:

- hyperlink start
- hyperlink end

Status:

- done
- implementation audit:
  [2026-04-19-p5-b-hyperlink-dispatch.md](/Users/haoxiang/Workspace/moonbit/feihaoxiang/ghostty/docs/plans/2026-04-19-p5-b-hyperlink-dispatch.md)

### P5.B3 Remaining high-frequency additions after `P4.C2`

When `P4.C2` lands more parser outputs, wire:

- semantic prompt
- any other newly translated high-frequency OSC commands

Status:

- todo

### P5.B4 Long-tail additions after P4.D

When `P4.D` lands more parser outputs, wire:

- clipboard contents
- kitty color protocol
- desktop notifications / ConEmu progress
- kitty clipboard protocol
- context signal
- kitty text sizing
- other translated long-tail OSC commands

Status:

- todo

## Completion criteria for P5.B

`P5.B` is complete only when all of the following are true:

- all `P5.B*` sub-slices are `done`
- every translated `osc.Command` variant is either:
  - routed to a typed `StreamAction`, or
  - intentionally silent because upstream stream behavior is logging-only
- latest `P5.B` code slice passes:
  - `moon check`
  - `moon test`
  - `moon coverage analyze`
  - `moon fmt`
  - `moon info`
- coverage residuals in touched files are either eliminated or explicitly
  accepted in the slice audit
- `.mbti` public-surface diffs are reviewed and intentional
