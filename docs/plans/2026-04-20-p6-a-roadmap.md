# P6.A Style And Attributes Roadmap

## Goal

Translate the terminal-side style and attribute state needed by
`stream_terminal.zig` without pulling cursor, screen, or PTY-response behavior
into the same commit batch.

## Upstream files

- `upstream/ghostty/src/terminal/style.zig`
- `upstream/ghostty/src/terminal/Screen.zig`
- `upstream/ghostty/src/terminal/Terminal.zig`

## MoonBit target files

- `src/terminal/terminal_style.mbt`
- `src/terminal/terminal_style_test.mbt`
- `src/terminal/pkg.generated.mbti`
- `docs/plan.md`
- `docs/plans/2026-04-20-p6-a-roadmap.md`

## Planned slices

### P6.A1 Style core and SGR application

Status:

- done
- implementation audit:
  [2026-04-20-p6-a1-style-core.md](/Users/haoxiang/Workspace/moonbit/feihaoxiang/ghostty/docs/plans/2026-04-20-p6-a1-style-core.md)

Scope:

- stream-facing terminal style value
- style color source tracking
- SGR attribute application/reset rules
- blackbox tests for default/reset/color behavior

### P6.A2 Dynamic palette and terminal colors

Status:

- todo

Scope:

- terminal palette wrapper state
- dynamic foreground/background/cursor colors
- reset/set behavior used by color operations

### P6.A3 Protected mode, status display, and mouse-shape state

Status:

- todo

Scope:

- protected-mode state carriers
- status display state
- mouse-shape storage
- closeout audit for the style lane

## Review notes

- `P6.A1` intentionally stops before palette mutation APIs
- `P6.A2` should reuse existing translated color types rather than inventing a
  second color model
- `P6.A3` closes the lane once the style-facing state needed by `P6.1` exists
