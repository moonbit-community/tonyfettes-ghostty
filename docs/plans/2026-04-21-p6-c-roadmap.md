# P6.C Screen / Page / Hyperlink Roadmap

## Goal

Split the screen-facing terminal model work into green slices that can land
 before page editing and scrolling are fully translated.

## Upstream files

- `upstream/ghostty/src/terminal/Screen.zig`
- `upstream/ghostty/src/terminal/page.zig`
- `upstream/ghostty/src/terminal/Terminal.zig`

## Planned slices

### P6.C1 Hyperlink and semantic metadata state

Status:

- done

Scope:

- active OSC8 cursor hyperlink state
- semantic prompt metadata (`seen`, click mode, cursor semantic content)
- prompt-row markers needed by later screen mutation logic

### P6.C2 Screen title and auxiliary metadata

Status:

- done

Scope:

- terminal title storage
- semantic-prompt redraw flag storage

### P6.C3 Cell and row backing values

Status:

- done

Scope:

- row prompt markers and row wrap flags
- cell content/style/hyperlink/semantic markers in MoonBit value form

### P6.C4 Editing and scrolling carriers

Status:

- todo

Scope:

- printable writes
- erase/insert/delete operations
- scroll-region mutation behavior
- DECALN and full-reset-facing state resets

Planned sub-slices:

#### P6.C4.1 Grid container and blank-state helpers

Status:

- todo

Scope:

- screen/page-owned row and cell arrays
- blank row/cell construction helpers
- coordinate-safe access paths needed by later mutation slices

#### P6.C4.2 Printable write carriers

Status:

- todo

Scope:

- print and repeat-print mutation helpers
- pending-wrap and wide-cell stamping behavior
- row flag updates caused by printable writes

#### P6.C4.3 Erase, insert, and delete carriers

Status:

- todo

Scope:

- erase-in-display and erase-in-line mutations
- erase chars, insert blanks, insert lines, delete chars, delete lines
- protected-cell handling and blank-cell replacement rules

#### P6.C4.4 Scroll, DECALN, and full-reset carriers

Status:

- todo

Scope:

- scroll up/down within margins
- DECALN fill behavior
- screen-facing full-reset cleanup on the model side

## Review notes

- `P6.C1` intentionally stops before any row/cell storage so the semantic and
  hyperlink metadata can land without fake page allocators
- `P6.C3` and `P6.C4` should stay tightly coupled to the exact upstream
  mutation semantics, because `P6.1.C` depends on them for faithful action
  handling
- `P6.C1` is now landed; `P6.C2` is the next smallest screen-owned slice that
  can move the model forward without inventing placeholder row storage
- `P6.C2` is now landed; `P6.C3` is the next required carrier slice before
  screen mutation bridge work can apply printable, erase, or scroll actions
- `P6.C3` is now landed; `P6.C4` is the remaining model slice before
  `P6.1.C` can wire screen mutation actions end to end
- `P6.C4` is intentionally decomposed before implementation so each mutation
  cluster can land in a green state with focused tests and coverage review
